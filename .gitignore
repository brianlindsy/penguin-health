# Lambda deployment
lambda-package.zip
*.zip

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.claude/

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
.temp/

#Lambda Test Events
test-events/

#Org config
config/

infra/config.py

# CDK synth output — regenerated on every `cdk synth`; never commit
cdk.out/

# Local environment files — only .env.example should be tracked
.env
.env.local
.env.*.local

# Scratch scripts for local PHI-touching testing — must never be
# committed. Anything that reads a real note PDF or its extracted text
# lives here.
scripts/scratch/