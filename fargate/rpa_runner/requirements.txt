# Pin specific versions so the image builds reproducibly. Bump
# deliberately — Playwright's Python package must match the base image's
# bundled browser binary version (see Dockerfile FROM tag).
playwright==1.49.0
boto3==1.35.0
